import 'package:flutter/material.dart';

class FaceBookLogo extends StatelessWidget {
  const FaceBookLogo({
    Key key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Image.asset('assets/images/facebook.png', height: 160);
  }
}
